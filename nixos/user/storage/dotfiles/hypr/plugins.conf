
plugin {
    hy3 {
        # Enable hy3 features without overriding existing behavior
        # no_gaps_when_only = false  # Keep your existing gap settings
        # autotile = false           # Don't auto-enable, let user choose
        # tab_group_height = 15      # Reasonable default
    }
}

plugin {
    hyprspace {
        overview {
            auto_exit = true
            auto_scale = true
            centered = true
            hide_top = true
            hide_cursor = false
            allow_workspace_click = true
            enable_gesture = false
            gesture_distance = 300
            gesture_direction = 2
        }
    }
}
