general {
    gaps_in = 5
    gaps_out = 20

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # ===== Bezier curves =====
    # Fast workspace motion
    bezier = quick,          0.12, 0,    0.18, 1

    # Smooth window motion (open / close / swap)
    bezier = windowEase,     0.22, 1,     0.36, 1

    # Subtle fade
    bezier = fadeEase,       0.4,  0.0,   0.6,  1.0

    # Linear (loops, borders)
    bezier = linear,         0,    0,     1,    1

    # ===== Workspace =====
    animation = workspaces,  1, 2, quick, slide

    # ===== Windows =====
    # Creation / destruction
    animation = windowsIn,   1, 7, windowEase, popin 90%
    animation = windowsOut,  1, 4, windowEase, popin 90%

    # Moving / swapping inside workspace
    animation = windowsMove, 1, 4, windowEase, slide

    # Fallback window animation
    animation = windows,     1, 4, windowEase

    # ===== Fading =====
    animation = fadeIn,      1, 4, fadeEase
    animation = fadeOut,     1, 2, fadeEase
    animation = fade,        1, 2, fadeEase

    # ===== Layers (rofi, notifications, etc.) =====
    animation = layersIn,    1, 4, fadeEase, fade
    animation = layersOut,   1, 3, fadeEase, fade
    animation = fadeLayersIn,  1, 3, fadeEase
    animation = fadeLayersOut, 1, 2, fadeEase

    # ===== Borders =====
    animation = border,      1, 2, quick
    animation = borderangle, 1, 15, linear, loop

    # ===== Global fallback =====
    animation = global, 1, 4, default
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}

